specVersion: "v1"
serviceId: "tester"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tester-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
